# This is Git's per-user configuration file.
[user]
	name = Elias Roa
	email = elias@roa.email

[diff]
    colorMoved = default

[difftool]
    prompt = false

[merge]
    conflictstyle = diff3

[mergetool]
    prompt = true

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only --features=interactive

[delta]
    features = unobtrusive-line-numbers decorations side-by-side hyperlinks navigate dracula

[delta "interactive"]
    keep-plus-minus-markers = false

[delta "decorations"]
    commit-decoration-style = blue ol
    commit-style = raw
    file-style = omit
    hunk-header-decoration-style = blue box
    hunk-header-file-style = red
    hunk-header-line-number-style = "#067a00"
    hunk-header-style = file line-number syntax
